Let's say we place a prism between black and white, so that there will be dispersion of light and deviation depends upon the angle prism is making but what if it is being connected to some configuration where we are passing certain bits inside electrical signals which sets a particular configuration to show on the screen.

For eg, I was just wondering when we press a keyboard key, say A letter, so based on its ASCII value, 65, we might convert it to binary and send a signal to the prism to set configuration in a way that results in the configuration of prism in a way letter A is being formed. Actually, we can't do it at this level, this dimension or this state, we need to manage the states properly that obviously pixel is something which is responsible for at what level the prism light split to generate the color we want and the electric signal like 65 in binary that we pass sets the certain prism configuration in a way that generates character A.

We might think that we need OS to build and also assembly language and so on but we haven't realized that what if we directly map our high level language to machine language without any assembly language, it's just we need to be certain about the configuration about how we can set it in a way that we don't have to consider assembly.

For eg, we have written #define, these bits are being defined in a particular sequence as a stream such that this stream wants us to define something, now for that we need to have something which sets the developer mode bit to be 1 and then inside that mode, we can give a stream command #define in sequence to define something, like A or anything like that.

Similarly, when we write cin>>, then this bit stream would ask for input, like if such bit stream is passed, then show Input: because Input: is mapped to cin>>, so we can write anything related to cin and when we write something like cout<<, then it passes stream to fetch all the variables that outputs all the cout, but where is it saving cin and cout. For eg, we have two spins, one in clockwise and other in anticlockwise like for eg, I want a read, I would pass a bit 0 to spin in anticlockwise direction and if I want a write, I would pass a bit 1 to spin clockwise direction.

Now, for eg, I want to read a file and save it to disk, I would store it at certain position which comes when disk rotates to a certain position, and it is similar to all the reads that happens and speed depends upon the context on from where we need to extract it. So this combination of speed and position is what decides which file and what part we are extracting. Then to write them up, we will make the movement clockwise.

It would be better if we find a way out that can easily replace software engineers and other engineers, but AI should not replace them, because maybe we don't need assembly, compilers or anything, maybe if we think in correct direction, we could easily get rid off too much programming and software engineering so that we can move on to build some new problem to solve as most are already being bored of software engineering.








